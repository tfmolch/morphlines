Stratio Wikipedia Cleaner Morphlines
=======================

The stratio wikipedia cleaner command cleans the wikipedia markup from a text.

Example:

``` 
{
  wikipediaCleaner {
    input : input_field
    output : output_field
  }
}
``` 